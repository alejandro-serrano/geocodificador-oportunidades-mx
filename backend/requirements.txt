# Backend del geocodificador.
#
# Instalar SIEMPRE con el Python del laboratorio, no con el del sistema:
#
#   ~/bdp/portable/python/bin/python3 -m pip install -r requirements.txt
#
# El laboratorio ya trae elasticsearch 8.13.2 y requests; se declaran aquí
# para que el archivo describa por completo lo que necesita el backend.

flask>=3.0
flask-cors>=4.0
elasticsearch>=8.0,<9.0
requests>=2.31
